pcre2_ast:
  language: c++
  engine: libFuzzer
  steps:
  - run: apt update
  - run: apt install -y cmake
  - run: mkdir build && cd build && cmake .. && make
  - run: $CXX $CXXFLAGS -std=c++17 fuzzer.cc -o $OUT/fuzzer $LIB_FUZZING_ENGINE libpcre2_ast.a
      -lpcre2-8
